--tworzenie tabeli Pilkarz (creation of the table Football player)

CREATE TABLE PILKARZ
(ID NUMBER, 
IMIE VARCHAR2(30), 
NAZWISKO VARCHAR2(60),  
DATA_URODZENIA DATE, 
PENSJA NUMBER
) ;

--tworzenie tabeli Mecz (creation of a table Match)

CREATE TABLE MECZ
(ID_MECZ NUMBER, 
DATA_ROZEGRANIA DATE, 
WYNIK_GOSPODARZA NUMBER, 
WYNIK_GOSCIA NUMBER
) ;

-- tworzenie tabeli Druzyna

CREATE TABLE DRUZYNA
(ID_DRUZYNY NUMBER, 
NAZWA_DRUZYNY VARCHAR2(60),
NAZWA_STADIONU VARCHAR2(60),
LIGA VARCHAR2(30)
) ;

-- tworzenie tabeli Miejscowosc

CREATE TABLE MIEJSCOWOSC
(ID_MIEJSCOWOSC NUMBER,
Nazwa_MIEJSCOWOSCI VARCHAR2(60),
NAZWA_STADIONU VARCHAR2(60)
) ;
-- tworzenie tabeli Gra
CREATE TABLE GRA
(ID_PILKARZA NUMBER,
ID_MECZ NUMBER,
POZYCJA VARCHAR2(30),
LICZBA_STRZELONYCH_BRAMEK NUMBER
) ;

-- wstawanie 10 rekordów do tabeli druzyna


--zatwierdzenie polecenia insert
commit;
